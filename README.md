# mongodb-learning

## Prerequisites

- A MongoDB Atlas cloud or local deployment
- dotnet

## Setup

Download a file `atlas-credentials.env` from the atlas cloud with the following environment variables:

```
MONGODB_USERNAME="<username>"
MONGODB_PASSWORD="<password>"
MONGODB_URI="<uri of database>"
```

## Usage

`dotnet watch`
